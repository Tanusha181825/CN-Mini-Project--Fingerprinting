# CN-Mini-Project--Fingerprinting
Web Server Fingerprinting Tool built using Python sockets that identifies server type and version by analyzing HTTP/HTTPS response headers, with SSL/TLS support and multithreaded scanning.
